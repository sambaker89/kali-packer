{
  "variables": {
    "vm_name": "kali-desktop",
    "qemu_binary": "/usr/bin/qemu-system-x86_64",
    "version": "0.0",
    "disk_size": "40960",
    "iso_checksum": "5e790f78ba091387ccb403b4235d8660e2832b02e63d618ee11bc5627f3c2d8e",
    "iso_checksum_type": "sha256",
    "iso_url": "http://http.kali.org/kali/dists/kali-rolling/main/installer-amd64/current/images/netboot/mini.iso",
    "box_name" : "kali-linux-2.0-amd64-desktop.box",
    "box_desc" : "Kali Linux 2.0 64 Bit Desktop"
  },
  "description": "{{user `box_desc`}}",
  "builders": [
    {
      "type": "qemu",
      "name": "kali-linux-{{user `version`}}-libvirt",
      "qemu_binary": "{{user `qemu_binary`}}",
      "vm_name": "{{user `vm_name`}}",
      "iso_url": "{{user `iso_url`}}",
      "iso_checksum": "{{user `iso_checksum`}}",
      "iso_checksum_type": "{{user `iso_checksum_type`}}",
      "disk_size": "{{user `disk_size`}}",
      "ssh_wait_timeout": "60m",
      "shutdown_command": "",
      "disk_size": "{{user `disk_size`}}",
      "format": "qcow2",
      "headless": false,
      "accelerator": "kvm",
      "http_directory": "http",
      "ssh_username": "root",
      "ssh_password": "vagrant",
      "net_device": "virtio-net",
      "disk_interface": "virtio",
      "boot_command": [
        "<esc><wait>",
        "auto url=http://{{ .HTTPIP }}:{{ .HTTPPort }}/preseed.cfg <wait>",
        "locale=en_US <wait>",
        "keymap=us <wait>",
        "<enter><wait>"
      ], "qemuargs": [
           [ "-m", "1024m" ],
           [ "--cpu", "host" ],
           [ "--no-acpi", "" ],
           [ "-vga", "qxl" ]
      ]
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "scripts": [
        "scripts/kali/update.sh",
        "scripts/common/sshd.sh",
        "scripts/kali/networking.sh",
        "scripts/common/vagrant.sh",
        "scripts/kali/cleanup.sh",
        "scripts/common/minimize.sh"
      ],
      "pause_before": "10s"
    }
  ],
  "post-processors": [
    {
      "type": "vagrant",
      "output" : "builds/{{user `box_name`}}"

    }
  ]
}
